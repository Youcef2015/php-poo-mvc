{% extends'template.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <h1>Hello world !</h1>
            <ul>
                {% for foo in foos %}
                    <li>{{ foo.name }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}